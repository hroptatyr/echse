%{
typedef enum {
	ECHS_LISP_UNK,
	ECHS_LISP_COLLECT,
	ECHS_LISP_DISCARD,
} echs_lisp_fun_t;
%}
%7bit
%readonly-tables
%switch=1
%ignore-case
%compare-strncmp
%compare-lengths
%enum
%struct-type
%define hash-function-name echs_lisp_funh
%define lookup-function-name echs_lisp_fung
%define slot-name kstr
%null-strings

struct echs_lisp_fung_s {
	const char *kstr;
	echs_lisp_fun_t klit;
};

%%
collect, ECHS_LISP_COLLECT
discard, ECHS_LISP_DISCARD
